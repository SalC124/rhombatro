extends Resource

const CARD_DRAW_SPEED: float = 0.1
const DEFAULT_CARD_MOVE_SPEED: float = 0.1

# const CARD_WIDTH: int = (1600 - (DEFAULT_HAND_SIZE * (71 * 2))) / 8
const CARD_WIDTH: int = 160

enum SUIT {
	Heart = 0,
	Club = 1,
	Diamond = 2,
	Spade = 3,
}

enum RANK {

}

const DEFAULT_HAND_SIZE: int = 8
